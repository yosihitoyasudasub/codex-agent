server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA フォールバック
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API プロキシ（backend が存在する場合）
    location /api/ {
        proxy_pass http://api:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
